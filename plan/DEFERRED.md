# DEFERRED — decisões conscientes de adiamento

> Ledger único (PLAN §6 / Rev2-M10) das features **conscientemente adiadas** durante a implementação das fases F0–F8. Cada item traz o **porquê**, o **ponytail:** (andaime/ceiling) e a **condição/data de reavaliação**. Adiar é decisão rastreável — não lacuna acidental. Data desta rodada: **2026-06-24**.

| # | Item | Fase | Por que adiado | `ponytail:` / ceiling | Reavaliar quando |
| --- | --- | --- | --- | --- | --- |
| ~~D1~~ | ~~Autenticação server-side~~ | F8.T2 | **ENTREGUE 2026-06-24** — middleware bearer opt-in (`SWARM_AUTH_TOKEN`), guarda `/api/*`+`/ws`, comparação constant-time, `/health` aberto. Testado. | — | — |
| D2 | **Escalonamento recursivo filho→pai→produto→usuário** (§9.3, F6.T1/PF9) | F6 | O núcleo HITL (`ask_human` → chat → `WAITING(human)` → resposta → timeout→`SUSPENDED`) está implementado e testado. O roteamento recursivo pela árvore de agentes (pai responde em escopo isolado, escala se não souber) é um mecanismo sofisticado de ator que exige um modo de run "answer-scope". | `ponytail:` meio-construir o answer-scope mentiria; melhor o canal humano direto, que cobre o caso comum. | Quando surgir um caso real de filho precisando de contexto que só o pai tem (não o humano). |
| D3 | **Despertar do pai em escopo isolado** (§9.3, F6.T2) | F6 | Depende de D2 (modo de run que responde só à pergunta sem reavaliar conclusão global). | `ponytail:` sem modo de run novo até D2 ser necessário. | Junto com D2. |
| D4 | **Reset+handoff de contexto** (§3.2, F2.T7) | F2 | A reidratação por índice (scope-spec/progress-log/env-resume + cauda de chat) + compactação in-place (`chooseContextStrategy`) cobrem a gestão de contexto atual. Reset com handoff só se justifica sob "ansiedade de contexto", dependente de modelo (§1.2). | `ponytail:` roteador escolhe `rehydrate`/`compact`; `reset` não é auto-selecionado. | Quando um modelo em uso mostrar ansiedade de contexto medível em runs longos. |
| D5 | **Resume real da sessão Pi (reabrir checkpoint intra-epoch)** (F2.T6/F3.T5) | F2/F3 | Decisão tomada: **reidratação textual** (rota b). O dead code (`piSessionFile`/`saveSession`) foi **removido**. O checkpoint por-epoch é observável via `HEARTBEAT`(`checkpointSeq`); a recuperação retoma do último run não-completo (granularidade de epoch), não do início do card. | `ponytail:` reidratação textual; sem `SessionManager.open` persistido. | Se a validação viva mostrar que reabrir a sessão Pi reduz tokens/turno de forma significativa. |
| D6 | **Smoke de UI em browser real (Playwright)** (F7.T8) | F7 | Requer browser no ambiente (não garantido em CI). Os componentes têm testes vitest/jsdom (55 testes) e o fluxo crítico é coberto por e2e de borda real (HTTP+WS). | `ponytail:` 1 spec de smoke quando o browser estiver disponível, não uma suíte completa. | Quando o CI tiver browser headless instalado. |
| D7 | **Execução do run de validação viva DeepSeek** (F0.T7/F8.T4) | F0/F8 | O **harness está implementado** (`validation/live-deepseek.live.test.ts`, gated por `DEEPSEEK_API_KEY`/`SWARM_VALIDATION=1`, skip limpo sem chave). O que falta é **operacional, não de implementação**: rodar contra o provider real exige chave + rede, indisponíveis no ambiente offline. Não é um corte de escopo — é um passo de execução. | `ponytail:` opt-in network-gated, fora do CI default. | Sempre que houver chave: `npm run validate:live`. |
| ~~D8~~ | ~~Cenários B/C + Cenário A e caos~~ | F8.T3/T5/T6 | **ENTREGUE 2026-06-24** — `scenarios.e2e.test.ts` (A com gate+artefato via HTTP, B e C com rotas encurtadas) e `resilience.e2e.test.ts` (crash-resume, log corrompido, falha composta ceiling+timeout). | — | — |
| D9 | **Idempotência por chave explícita para `create_subtask`/`create_artifact`** (F4.T5) | F4 | Hoje a dedupe é por conteúdo (`create_subtask`), por path (`create_artifact`) e `post_message` ficou idempotente por última-linha. Uma chave de tool-call explícita no run é mais robusta mas não há duplicação observada nos testes de crash-resume. | `ponytail:` os guards de conteúdo/path cobrem o caso prático; chave explícita só se um teste de caos pegar duplicação. | Se um teste de caos (F8.T5) flagrar efeito duplicado. |

## Notas

- **Reavaliação periódica (§1.2).** Os andaimes de governança (gate de avaliação, monitor de convergência, reset/compactação) codificam suposições que expiram conforme os modelos melhoram. Revisar este ledger a cada troca de modelo e remover o que deixou de carregar peso.
- O gate de avaliação independente (F5) é **opt-in** (`SWARM_EVALUATION_GATE=1`) justamente por ser uma suposição que expira: para tarefas dentro da zona de competência do modelo ele vira sobrecusto (§8.2).
