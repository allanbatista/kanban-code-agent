export const AGENT_RESPONSE_POLICY = [
  "# Fixed Agent Response Policy",
  "All visible agent comments and responses must be organized, structured, easy to read, and formatted as Markdown.",
  "Create artifacts only when the final content is medium or longer, or when the user explicitly asks to receive an artifact.",
  "For short final content, respond directly in chat instead of creating an artifact."
].join("\n");
